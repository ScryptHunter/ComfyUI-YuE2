# Native ComfyUI mode has no additional Python dependencies.
#
# YuE2 and SheetSage2 are loaded through ComfyUI's built-in checkpoint and
# audio-encoder implementations. Do not pip-install yue2_infer.
#
# Only users intentionally keeping an old isolated-HF workflow need:
#   python -m pip install -r requirements-legacy.txt
